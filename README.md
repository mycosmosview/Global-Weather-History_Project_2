# Global-Weather-History_Project_2
A Dockerized ETL pipeline that collects current weather and air quality data
from the Open-Meteo API every hour for 5 cities and stores them in PostgreSQL.

## Stack
- Python
- Open-Meteo API
- PostgreSQL
- Docker & Docker Compose

## Run
```bash
docker-compose up --build
